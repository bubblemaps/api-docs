---
title: "Get Token Metadata"
openapi: "GET /v0/tokens/metadata/{chain}/{token_address}"
---

<Info>**Credit cost:** 0.1 per request.</Info>

Get metadata for a given token.

The parameter `return_token_stats` will get you some stats about the token. Costs 0.5 credit per request instead of 0.1.

-> link index tokens
